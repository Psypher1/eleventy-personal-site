---
title: Manor
layout: pageLayout
tags: page
order: 1
---


{% PageHeading heading="Hi, I'm James Dante", subheading="I am a Wizard(Developer), Curator(Writer) and Professor(Tutor). I believe that knowledge should be shared, and I try to do my part in that regard each day. I am on a journey to become a reknown <span class='font-bold'>Potions Master</span>" %}

{% include "partials/_social.njk"%}

<h2 class="text-center text-2xl md:text-3xl mt-16 font-heading tracking-wider">I solemnly swear that I am up to no good...</h2>

{% PageSection "My Pensieve"%}
<p class="prose text-green-100">To settle my mind, I move my thoughts to my pensieve. Have a look at some of them...</p>

{# Parchment #}
<article class="mt-5">
    <h3 class="font-headingAlt text-xl md:text-2xl mb-4  font-semibold text-gray-100">Latest Parchment</h3>

  {% for article in articles %}

    {% if article.title == "Learn 11ty Part 8" %}
        <a href="https://dantedecodes.hashnode.dev/{{article.slug}}"
        target="_blank"  rel="noopener noreferrer"
        class="mb-6 text-gray-300 bg-gray-900 p-4 block rounded hover:bg-black transition duration-300"
        >  
        <h4 class="text-xl md:text-2xl font-headingAlt text-gray-200">{{article.title}}</h4>
 
        <p class="prose text-gray-300 prose-sm md:prose-base ">{{article.brief}}</p>

       </a>
    {% endif %}
   
  {% endfor %}
</article>

{# Potion #}
<article class="mt-12">
<h3 class="font-headingAlt text-xl md:text-2xl mb-4 font-semibold text-gray-100">A Potion You Should See</h3>
 
 {% for item in potions %}
  {% if item.title == "The Language Hub" %}
    <a href="{{item.visit}}" target="_blank" rel="noopener noreferrer nofollow" class="bg-gray-900 hover:bg-black shadow-xl p-6 block rounded-md text-center prose text-gray-100 prose-headings:text-gray-100 transition duration-300">
       {# <img src="{{item.image}}" alt="{{item.title}}" width="{{item.imageWidth}}" height="{{item.imageHeight}}" class="block rounded"> #}
      <h4 class="text-xl md:text-2xl   font-headingAlt ">
      {{item.title}}
      </h4>
      <p>{{item.description}}</p>

    </a>
  {% endif %}
 {% endfor %}

</article>
{% endPageSection %}


{% PageSection "People I've Worked With"%}

 {% for item in testimonials %}
<blockquote class="p-4 border-l-4 bg-black text-gray-100 border-gray-400 mb-8 rounded">
  <p class="mb-2  prose prose-base text-gray-300">"{{item.message}}"</p>
  <cite> - 
  <a href="{{item.link}}" class="text-gray-400 hover:text-white transition duration-300" target="_blank" rel="noopener noreferrer">{{item.name}}</a>
  </cite>
  </blockquote>
{% endfor %} 

{% endPageSection%}



{# 
{% PageSection "People I've Worked With"%}
<blockquote class="p-4 italic border-l-4 bg-neutral-900 text-neutral-100 border-neutral-500 quote">
   <p class="mb-2 prose text-neutral-100">"Dante goes above and beyond expectations each time. He will call you to verify things or run ideas past you. And he will make sure everything runs as efficiently as possible. He NEVER forgets a discussion, which is great because nothing falls through the cracks. Highly recommend."</p>
    <cite> - 
        <a href="https://twitter.com/tapiwanashe36" target="_blank" rel="noopener noreferrer">Tapiwanashe</a>
    </cite>
</blockquote>
{% endPageSection %} #}

    









{# {% Section content="I am section content"%} #}

{# Привет! я #}


